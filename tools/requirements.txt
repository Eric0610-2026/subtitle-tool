 # 字幕生成与双语翻译工具 - Python 依赖
 # 安装: pip install -r tools/requirements.txt
 # GPU 加速（需 CUDA 12.x + cuDNN）：
 #   pip install torch --index-url https://download.pytorch.org/whl/cu121
 #   然后再执行: pip install -r tools/requirements.txt

 torch>=2.0
 PySide6>=6.5
 faster-whisper>=1.0
 opencc-python-reimplemented  # 繁简转换，不含该包则使用内置简易转换表
 winotify                      # Windows 通知（可选，有 PowerShell 回退）
