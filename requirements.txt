 # 字幕生成与双语翻译工具 - Python 依赖
 # 安装: pip install -r requirements.txt
 # PyTorch（CUDA 版推荐，CPU 版也可）：
 #   pip install torch --index-url https://download.pytorch.org/whl/cu121
 #   或仅 CPU：pip install torch

 PySide6>=6.5
 faster-whisper>=1.0
 opencc-python-reimplemented  # 繁简转换，不含该包则使用内置简易转换表
 winotify                      # Windows 通知（可选，有 PowerShell 回退）
