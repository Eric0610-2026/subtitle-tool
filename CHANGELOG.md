# 更新日志

## v1.2 — 2026-07-31

### ✨ 新功能

- **模型配置整合到二级页面** — 设置页面中的 API URL、API Key、模型选择、检测功能整合到独立的 `ModelConfigDialog` 二级对话框中，主页面仅保留配置按钮和状态指示器（绿色圆点=已配置）
- **模型列表搜索过滤** — 模型选择对话框支持实时搜索过滤，输入即过滤，大小写不敏感
- **从 API 获取可用模型列表** — 点击「获取列表」按钮，自动调用 `/v1/models` 端点获取可用模型列表，支持 OpenRouter（364 个模型）、DeepSeek、商汤等标准 OpenAI 兼容 API
- **API 配置检测功能** — 填写 API URL/Key/模型后，点击「检测」按钮即可验证配置是否可用，成功/失败均有明确提示（后台线程，不阻塞 UI）
- **一次性发送全部文本** — 新增复选框，勾选后不拆分批次，所有去重文本通过一次 API 调用发送
- **模型名称改为可编辑下拉框** — 支持从列表选择或手动输入模型名称

### 🔧 修复

- **修复 Python 3.10 兼容性 Bug** — `concurrent.futures.TimeoutError` 在 Python 3.10 上不是内置 `TimeoutError` 的子类，导致超时异常未被捕获、翻译流程崩溃。现已兼容 Python 3.10+ 全版本
- **修复 OpenRouter API URL 配置错误** — 缺少 `/chat/completions` 路径，导致 API 调用失败
- **修复设置页面初始化崩溃** — `_update_cfg_btn_text()` 在控件创建之前被调用，导致 `AttributeError`

---

## v1.1 — 2026-07-20

### ✨ 新功能

- **多 LLM 方案管理** — 支持在设置中管理多个翻译 API 方案（添加/删除/切换），每个方案可独立配置 API URL、Key 和模型
- **嵌入字幕对话框** — 支持批量选择视频+字幕文件，自动匹配同名文件，一键嵌入 MKV
- **嵌入前暂停确认** — 翻译完成后弹出对话框，可预览/编辑字幕内容后再嵌入 MKV
- **Whisper 模型持久驻留** — 模型在首次加载后常驻内存/显存，后续文件无需重新加载；支持手动卸载释放显存
- **启动预加载模型** — 应用启动后后台预加载 Whisper 模型，让第一次处理时秒开
- **字幕质量检查** — 自动检测字幕空行、过短/过长、时间重叠、间隙过长等问题并生成报告
- **漏译补翻** — 批量翻译后检测未译句子，自动逐条补翻
- **过滤空字幕条目** — 转写/翻译后自动过滤空文本条目
- **字幕备份** — 翻译前自动备份原始字幕至 `srt_backup` 目录
- **缓存语言检测** — `auto` 模式下同批后续文件跳过语言检测，复用首次检测结果
- **翻页编辑弹窗** — 预览区支持翻页式字幕编辑，方便逐条修改
- **预览区拖放** — 支持文件拖放到预览区加载
- **永久保存设置** — 设置对话框支持「本次有效」和「永久保存」两种模式
- **自动安装依赖** — 启动时自动检测并安装缺失的 pip 依赖
- **Fusion 风格文件对话框** — 文件选择改用更美观的 Fusion 风格对话框

### 🔧 修复

- 修复长时段字幕的两大核心 Bug（时间轴处理、段落拆分）
- 修复内嵌时缺少文件导致解包崩溃问题
- 修复强制切分不再产出空字幕
- 修复翻译进度条倒退问题
- 修复内嵌后清理临时文件
- 修复音频进度毫秒计算错误
- 修复全部缓存命中时返回译文而非原文
- 修复 Tab 1（字幕翻译）扫描时使用错误目录路径
- 修复替换 QInputDialog 为静默对话框，消除系统提示音
- 修复新用户部署的 5 个问题（torch 依赖、仓库 URL、弹窗提醒、清除默认路径）
- 修复字幕备份目录改用相对路径，移除硬编码绝对路径

### 🧪 测试

- 新增 translator/muxer/widgets 测试（44 个用例）
- Mock torch 规避 CUDA 初始化，测试从 5.7s 提速至 1.1s
- 总计 145 个单元测试，覆盖率大幅提升

### ♻️ 重构

- 统一模块命名：`zimu_app` → `subtitle_app`
- 拆分自定义 Qt 控件为独立 `widgets` 模块
- 拆分进度面板、预览面板、日志面板为独立 `panels` 模块
- 消除重复逻辑，提取公共方法
- 移除 Token/费用统计模块
- 修复 7 个架构与工程问题（死代码/None 守卫/配置重载/备份简化等）

---

## v1.0 — 2026-07-01

- 初始版本发布
- 基于 faster-whisper 的本地语音转写
- AI 翻译（兼容 OpenAI Chat Completions API）
- 双语字幕生成
- MKV 字幕内嵌
- 断点续转/续翻
- 并行流水线处理
- 批量文件处理
- 拖拽操作支持
- 暗色/亮色主题切换